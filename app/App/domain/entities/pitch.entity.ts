export interface Pitch {
    deporte: string;    // Cadena que representa el deporte, por ejemplo, "futbol"
    id_place: string;   // Cadena que representa el ID del lugar
    imageUrl: string;   // Cadena que representa la URL de la imagen
    nombre: string;     // Cadena que representa el nombre de la cancha
    precio: number;     // Número que representa el precio
    sena: string;       // Cadena que representa la seña (puede que sea un depósito o adelanto)
    tipo: string;       // Cadena que representa el tipo de cancha, por ejemplo, "5" para futbol 5
  }
  