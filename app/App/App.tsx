import { QueryProvider } from './infraestructure/providers/QueryProvider';

// ... other imports ...

export default function App() {
  return (
    <QueryProvider>
      {/* Your existing app content */}
    </QueryProvider>
  );
} 